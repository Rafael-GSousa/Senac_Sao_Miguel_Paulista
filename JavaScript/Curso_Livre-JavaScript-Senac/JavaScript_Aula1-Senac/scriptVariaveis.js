// Declarando uma variável
var nome = 'Rafael';

alert(nome);

nome = 'João';

alert(nome);

// O Javascript funciona de maneira sequencial de cima para BaseAudioContext,
//  então se há um erro no meio do código será executado até a linha anterior ao erro
// Podemos verificar o erro apertando a tecla F12 na tela do navegador, pois muitas vezes o javascript não aponta o erro, apenas para de executar